general:
  branches:
    ignore:
      - gh-pages

test:
  override:
    # - ./scripts/validate_html5.sh
    - echo "no tests"

deployment:
  production:
    branch: master
    commands:
      - ./scripts/deploy.sh
